import './Loading.css';
import handling from '../../../img/handling.svg';

export default function Loading() {
  return (
    <img
      className="Loading"
      src={handling}
      alt="loading process"
    />
  );
}
